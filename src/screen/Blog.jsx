import React from "react";

import { Container } from "react-bootstrap";
import Base from "../core/Base";
import InnerHeader from "../component/InnerHeader/InnerHeader";

const BlogComponent = () => {
  return (
    <Base>
      <InnerHeader title="Blog" />
    </Base>
  );
}

export default BlogComponent;
